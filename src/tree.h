//
// Created by Kaylee Briggs on 9/26/23.
//
#include "node.h"
#ifndef PROJECT1_TREE_H
#define PROJECT1_TREE_H

#endif //PROJECT1_TREE_H
class Tree {
    Node* root;
    // contains nodes
    Node* insert(Node* root, int ufId) {
        if (root == nullptr){
            return new Node(ufId);
        }
        if (key < root->left, ufId)
    };